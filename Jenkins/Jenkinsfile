pipeline {
    agent {  label "python-kubagent" }

    stages {
        stage('Build python project!') {
            steps {
                container('kaniko') {
                    echo 'Kaniko'
                    sh '''
                        /kaniko/executer --context `pwd`/docker --destination ralphseets/mijnkia-test:0.1
                    '''
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}