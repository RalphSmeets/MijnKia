podTemplate(inheritFrom: 'python-kubagent') {
    node(POD_LABEL) {
        stage('Build Java Image') {
            container('kaniko') {
                stage('Build a Go project') {

                    sh '''
                        ls -lrt
                        /kaniko/executor --context `pwd`/docker --destination ralphsmeets/mijnkia-kaniko:0.1
                    '''
                }
            }
        }

    }
}
